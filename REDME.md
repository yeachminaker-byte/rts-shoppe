# Ecomars

Minimal full-stack starter (HTML + CSS + JS + Python API) for GitHub + Vercel.

## Local tips
- Serve `index.html` with any static server (or open directly).
- API runs only on Vercel serverless path `/api/products`.